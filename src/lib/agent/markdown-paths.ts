/** Paths that support Accept: text/markdown (edge-safe; no Node imports). */
export const markdownNegotiablePaths = new Set([
  "/",
  "/changelog",
  "/privacy",
  "/terms",
  "/cookies",
  "/legal-notice",
  "/auth.md",
]);
